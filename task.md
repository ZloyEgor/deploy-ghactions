ЗАДАНИЕ:

Взять за основу проект `/Users/egorstukov/Developer/pyweb2025/task2.1`, дублировать его в текущем проекте. Создать свой шаблон (с помощью Jinja2 для mkdocs), выполнить сборку сайта с помощью GitHub Actions. 

Создать собственную тему на основе HTML, CSS, JS с использованием Bootstrap и React.

Разработать набор пайплайнов (yml-файл) для тестирования и  сборки статики (HTML, CSS, JS) — фронтэнда сайта, а затем построения собственно самого сайта (интеграции контента в разметке Markdown в шаблон сайта) и деплоя его на GitHub Pages. 
Необходимо учесть, что HTML файлы должны валидироваться на корректность, минифицироваться, должна быть предусмотрена сборка с помощью PostCSS (см. репозиторий).